import { RecentTransactionsResponse } from './recent-transactions-response';

describe('RecentTransactionsResponse', () => {
  it('should create an instance', () => {
    expect(new RecentTransactionsResponse()).toBeTruthy();
  });
});
