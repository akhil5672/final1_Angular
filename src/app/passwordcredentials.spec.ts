import { Passwordcredentials } from './passwordcredentials';

describe('Passwordcredentials', () => {
  it('should create an instance', () => {
    expect(new Passwordcredentials()).toBeTruthy();
  });
});
